<%
# Renders the workload data for one single issue.
# Parameters:
#  * assignee: The user or group to render the data for.
#  * summarizedWorkload Hash that contains the summarized workload for invisible issues.
%>

<% summarizedWorkload.keys.sort.each do |day| %>
  <%
     klass  = 'hours'
     klass += ' holiday' if summarizedWorkload[day][:holiday]
     klass += ' today'   if @today === day
     klass += ' ' + load_class_for_hours(summarizedWorkload[day][:hours], assignee)

     hoursString = (summarizedWorkload[day][:hours].abs < 0.01) ? '' : sprintf("%.1f", summarizedWorkload[day][:hours], assignee)
  %>
  <td class="<%= klass %>">
      <span>
        <%= hoursString %>
      </span>
  </td>
<% end %>